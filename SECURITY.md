# Security

Never commit a CV, `.env`, API key, Gmail app password, job history, or outreach
queue. The included `.gitignore` excludes these files by default.

If a secret is committed, revoke it immediately, create a replacement, and remove
the secret from Git history before publishing.

To report a vulnerability, open a GitHub security advisory rather than a public
issue.

